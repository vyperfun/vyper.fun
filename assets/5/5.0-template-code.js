'use-strict'

// TODO: require `it-pipe` module

// TODO: define const Protocol with the name of the chat Protocol

function send (stream) {
    // TODO: pipe the data from stdin to sink of the stream
}

function receive (stream) {
    // TODO: pipe the data from stream to the console
}

module.exports = {
    PROTOCOL,
    send,
    receive
}
